function calculate() {
    const fruits = []
    let fruitIndex = 0
   /* while (fruitIndex < fruits.length) {
        console.log(fruits[fruitIndex])
        fruitIndex++
    }

    for (let fruitIndex = 0; fruitIndex < fruits.length; fruitIndex++) {
        console.log(fruits[fruitIndex])
    }

    for (let fruitIndex in fruits) {
        console.log(fruits[fruit])
    }*/

    if (fruits.length == 0) {
            console.log("No fruit")
    } else {
        while (fruitIndex < fruits.length)  {
                console.log(fruits[fruitIndex])
            fruitIndex++
        } 

    }

    /*var fruits = "apple"
    if (fruits == "apple") {
        alert("This is apple")
    } else if (fruits == "guava") {
        alert("This is guava")
    } else {
        alert("Fruit not found")
    }
    switch (fruits) {
        
        case "apple":
            alert("This is apple")
            break;
        case "guava":
            alert("This is guava")
            break;
        default:
            alert("No fruit found")
       
    }
    
    var industry = "IT"
    
    if (industry == "IT" || industry == "HEALTCARE" || industry == "CONSTRUCTION") {
     alert("Field is shortage of workers")
    }

    if(["IT", "CONSTRUCTION", "HEALTCARE"].includes(industry) > 0) {
     alert("Field is shortage of workers")
    }

    switch(industry) {
         case "IT":
         case "CONSTRUCTION":
         case "HEALTCARE":
             alert("Field is shortage of workers")
             break
    }*/
}
/*var fruits = "apple"

if (fruits == "apple") {
    alert("This is apple")
} else if (fruits == "guava") {
    alert("This is guava")
} else {
    alert("Fruit not found")
}

switch (fruits) {
    case "apple": 
        alert("This is apple")
        break
    case "guava": 
        alert("This is guava")
    default:
        alert("Fruit not found")      
}

var person1 = { // this is example of an object
    weight: 60,
    height: 170,
    age: 40,
    eyeSight: {
        left: 0.5,
        right: 0.7
    },
    fruitsLiked: ["apple", "guava", "lemon", "orange"]
}

var person2 = { // this is example of an object
    weight: 50,
    height: 160,
    age: 50,
    eyeSight: {
        left: 0,
        right: 0
    },
    fruitsLiked: ["apple","orange"]
}


person1.eyeSight.left
var fruits = ["apple", "guava", "lemon", "orange"] // this is an example of an array
var numbers = [0, 1, 2, 4]
var mix = [0, "apple", 1, 2, "orange", person1]

fruits[0]
fruits[1]

var personList = [person1, person2]


[
    { // this is example of an object
        weight: 50,
        height: 160,
        age: 50,
        eyeSight: {
            left: 0,
            right: 0
        },
        fruitsLiked: ["apple","orange"]
    }, 
    { // this is example of an object
        weight: 50,
        height: 160,
        age: 50,
        eyeSight: {
            left: 0,
            right: 0
        },
        fruitsLiked: ["apple","orange"]
    }
]

// for, do while, while, foreach

let fruitIndex = 0

while (fruitIndex < fruits.length) {
    console.log(fruit[fruitIndex])
    fruitIndex++
} */


